CREATE TYPE "maritalstatus" AS ENUM (
  'single',
  'married'
);

CREATE TYPE "religion" AS ENUM (
  'islam',
  'christian',
  'budhhist',
  'animisme',
  'hindu',
  'other'
);

CREATE TYPE "sex" AS ENUM (
  'F',
  'M'
);

CREATE TYPE "statusadmission" AS ENUM (
  'registered',
  'submitted'
);
